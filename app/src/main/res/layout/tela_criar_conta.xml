<?xml version="1.0" encoding="utf-8"?>

<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorPrimary">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="@color/colorPrimary">

        <EditText
            android:id="@+id/user_email"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="50dp"
            android:hint="Email..."
            android:inputType="textEmailAddress"
            android:textColorHint="@color/branco"
            android:textColor="@color/branco"/>

        <EditText
            android:id="@+id/user_senha"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="50dp"
            android:hint="Senha..."
            android:inputType="textPassword"
            android:textColorHint="@color/branco"
            android:textColor="@color/branco"/>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="70dp"
            android:orientation="horizontal"
            android:layout_marginTop="35dp">

            <Button
                android:id="@+id/criar_conta"
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                android:textColor="@color/secundaria"
                android:gravity="center"
                android:text="Criar conta"
                android:textSize="20dp"
                android:background="@drawable/butao_pagina_inicial"
                />

        </LinearLayout>

        <TextView
            android:id="@+id/textinho"
            android:visibility="gone"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/branco"
            android:textSize="25dp"
            android:text="Um Email de confirmação foi enviado, você poderá logar após confirmá-lo."
            android:gravity="center"

            />

    </LinearLayout>

</ScrollView>
